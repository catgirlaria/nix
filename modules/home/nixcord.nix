{
  # Vesktop is broken with Electron version that `nixpkgs-unstable` has so im using flatpak instead
  # 	programs.nixcord = {
  # 		enable = true;
  # 		vesktop.enable = true;
  # 		discord.silenceNoModClientWarning = true;
  # 		config.plugins = {
  # 			# TODO: Add plugins
  # 		};
  # 	};
  # See: modules/system/flatpak.nix
}
